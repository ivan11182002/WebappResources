<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
  PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
  "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>

  <!-- 指定jdbc driver的位置 -->
  <classPathEntry location="C:\Users\carter\.m2\repository\mysql\mysql-connector-java\5.1.48\mysql-connector-java-5.1.48.jar" />

  <context id="DB2Tables" targetRuntime="MyBatis3">

    <commentGenerator>
       <!-- true 不要產生註解。 -->
       <property name="suppressAllComments" value="true" />
	   <!-- true 不要在註解裡產生timestamp。 -->
       <property name="suppressDate" value="true" />
    </commentGenerator>

	<!-- DB連線設定。 -->
    <!--<jdbcConnection driverClass="oracle.jdbc.driver.OracleDriver"
        connectionURL="jdbc:oracle:thin:@localhost:1521:xe"
        userId="mpclmuser"
        password="mpclmuser">
    </jdbcConnection>-->
    <jdbcConnection driverClass="com.mysql.jdbc.Driver"
        connectionURL="jdbc:mysql://192.168.14.203:3306/wwunion?useUnicode=true"
        userId="wwunion"
        password="wwunion123">
    </jdbcConnection>

    <!-- DB數字相位的對應。true 數字欄位都對應成BigDecimal，false MyBatis自動判斷。 -->
<!--    <javaTypeResolver >
      <property name="forceBigDecimals" value="false" />
    </javaTypeResolver> -->

	<!-- 產生 model object。targetPackage輸出的package。targetProject輸出的目錄。 -->
    <javaModelGenerator targetPackage="com.wwunion.backend.entity" targetProject="output">
    </javaModelGenerator>

	<!-- 產生 sqlMap檔(MyBatis Mapper XML設定檔)。targetPackage輸出的package。targetProject輸出的目錄。 -->
    <sqlMapGenerator targetPackage="com.wwunion.backend.dao"  targetProject="output">
    </sqlMapGenerator>

	<!-- 產生 mapper interface檔。targetPackage輸出的package。targetProject輸出的目錄。 -->
    <javaClientGenerator type="XMLMAPPER" targetPackage="com.wwunion.backend.dao"  targetProject="output">
    </javaClientGenerator>

	<!-- table element, 設定要產生的檔案的來源db table。 -->
	<!-- schema, DB schema。 -->
	<!-- tableName, Table的名稱。 -->
	<!-- domainObjectName, Model Object的名稱。 -->
	<!-- enableInsert, 是否要產生 insert 的 method。 -->
	<!-- enableUpdateByExample, 是否要產生 updateByExample 的 method。 -->
	<!-- enableUpdateByPrimaryKey, 是否要產生 updateByPrimaryKey 的 method。 -->
	<!-- enableDeleteByExample, 是否要產生 deleteByExample 的 method。 -->
	<!-- enableDeleteByPrimaryKey, 是否要產生 deleteByPrimaryKey 的 method。 -->
	<table tableName="BCH_ROLE_FUNCTION" domainObjectName="BchRoleFunction">
	</table>
	
  </context>
</generatorConfiguration>